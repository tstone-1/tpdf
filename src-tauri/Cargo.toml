[package]
name = "tpdf"
version = "26.8.6"
description = "Fast, lightweight PDF viewer and editor"
authors = ["tstone-1"]
edition = "2021"
# 1.86 for `OnceLock::wait`, which `edits.rs` uses to let an open return before
# the document's fingerprint has been hashed. Raised from 1.85 on 2026-08-19,
# and clippy is what noticed: `incompatible_msrv` compares this line against
# the APIs actually called, so the claim cannot drift from the code. Nothing
# builds against the minimum in practice --- `rust-toolchain.toml` pins 1.97.1
# and the `toolchain` gate asserts it --- which is exactly why the claim needs
# something checking it rather than somebody remembering.
rust-version = "1.86"
# The benchmark bins make `cargo run` ambiguous, and `tauri dev` shells out to a
# bare `cargo run`.
default-run = "tpdf"

[lib]
# The `_lib` suffix keeps the lib name distinct from the bin name on Windows.
# See https://github.com/rust-lang/cargo/issues/8519
name = "tpdf_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Already in the graph twice over (rustls via the updater, and tauri's own
# stack), so declaring it adds no package -- checked with `cargo tree`, which
# is the standing rule for anything the licensing constraint touches.
# MIT OR Apache-2.0.
sha2 = "0.10"

# PDF rendering and text extraction. Settled 2026-07-27 by spikes 0.1, 0.3 and 0.5:
# it renders, extracts and sandboxes correctly, and is not usable for redaction
# -- see AGENTS.md. `thread_safe` is a default feature and is left ON, but note it
# does NOT serialize calls behind a mutex despite what the crate README says: it
# only makes `Pdfium` Send + Sync and awaits the global bindings. Concurrent
# renders are undefined behaviour and crash (examples/thread_probe.rs). Parallelism
# comes from worker processes (spike 0.5), never from threads.
pdfium-render = "0.9"

# A reader's own search pattern, when the find bar is in regex mode. Already in
# the tree transitively through the toolchain, so declaring it added no package.
regex = { version = "1", default-features = false, features = ["std", "perf", "unicode"] }

# Low-level PDF object graph and content-stream operators.
lopdf = "0.44"

parking_lot = "0.12"
anyhow = "1"

# Tile encoding. `png` is one of the two formats the tile protocol serves --- see
# `TileFormat` in `render.rs` and `parse_format` in `protocol.rs` --- so it is
# linked into the shipping binary rather than being a spike leftover. What is
# still spike 0.1 is the *comparison*: the scroller and the thumbnail strip both
# ask for `raw`, and `autobench` is what measures the two against each other.
png = "0.17"

# getrusage, for peak RSS in the tile bench. Pdfium allocates natively, so a
# Rust allocator hook would report almost nothing.
libc = "0.2"
tauri-plugin-dialog = "2.7.2"

# Unicode case folding, for search. `char::to_lowercase` is the operation for
# *displaying* text and leaves `ß` alone --- it is already lowercase --- so
# `strasse` could not find `Straße`; folding is the operation Unicode defines for
# caseless *matching*, which is what a find bar does. Taken on 2026-08-01 as a
# decision rather than a fix, because the same operation folds `ﬁ` to `fi` and
# `search.rs` had refused to normalise ligatures.
#
# MIT, and its only new transitive is `unicode-normalization` (MIT OR Apache-2.0)
# with `tinyvec`/`tinyvec_macros` beside it --- checked with `cargo metadata` over
# the whole tree rather than read off a README, because one copyleft crate anywhere
# removes the option of making this repository public (AGENTS.md).
caseless = "0.2.2"
tauri-plugin-updater = "2.10.1"
cms = { version = "0.2.3", default-features = false }
der = { version = "0.7", default-features = false }
x509-cert = { version = "0.2", default-features = false }
quick-xml = "0.41"

# The macOS print dialog. PDFKit paginates the document and builds the
# NSPrintOperation; AppKit supplies NSPrintInfo and runs the panel. `objc2`,
# `objc2-foundation` and `objc2-app-kit` are already in the tree transitively via
# Tauri, so only `objc2-pdf-kit` is genuinely new --- same crate family, same
# `Zlib OR Apache-2.0 OR MIT`, so no licensing question (AGENTS.md).
[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"
objc2-foundation = { version = "0.3", features = ["NSData", "NSString", "NSURL"] }
objc2-app-kit = { version = "0.3", features = [
    "NSPrintInfo",
    "NSPrintOperation",
    # `noteNewRecentDocumentURL:`, which fills *File > Open Recent* and the Dock
    # icon's Recent Documents. See `recentdocs.rs`.
    "NSDocumentController",
] }
objc2-pdf-kit = { version = "0.3", features = [
    "PDFDocument",
    "PDFPage",
    "objc2-app-kit",
    # `annot-probe --mode preview`: the annotation list a parser that did not
    # write the file reports, and PDFKit drawing the page into a bitmap so the
    # mark can be shown to change pixels. Features on a crate already in the
    # tree, so no package is added and the licensing sweep is unmoved.
    "PDFAnnotation",
    "objc2-core-graphics",
] }
objc2-vision = { version = "0.3.2", default-features = false, features = ["VNRequest", "VNRecognizeTextRequest", "VNObservation", "VNRequestHandler", "VNTypes", "VNError", "std", "objc2-core-graphics", "objc2-core-foundation"] }
objc2-core-graphics = { version = "0.3.2", default-features = false, features = [
    "CGImage",
    "CGDataProvider",
    "CGColorSpace",
    # `CGBitmapContextCreate`, which is what `--mode preview` draws into. Both
    # names are needed: `CGContext` for the type, `CGBitmapContext` for the
    # module the constructor lives in.
    "CGContext",
    "CGBitmapContext",
    "std",
] }
objc2-core-foundation = { version = "0.3.2", default-features = false, features = ["CFBase", "std"] }

# Windows containment, for `examples/win_sandbox_probe.rs`. macOS gets its boundary
# from `sandbox_init`, which has no counterpart here: job objects, integrity
# levels and restricted tokens are all Win32, so there is no version of this that
# avoids a direct dependency. `windows-sys 0.61` is already in the tree
# transitively (Tauri, and several build scripts), so this adds no crate at all
# --- and it is `MIT OR Apache-2.0`, checked with `cargo metadata` rather than
# assumed, because one copyleft crate anywhere removes the option of making this
# repository public (AGENTS.md).
[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Security",
    # Toolhelp: `backend-probe` reads its own module list and finds its worker
    # children through the OS process table, which are the observables its
    # claims rest on -- see `mapped_images` and `worker_pids` there.
    "Win32_System_Diagnostics_ToolHelp",
    "Win32_System_Console",
    "Win32_System_JobObjects",
    "Win32_System_Memory",
    "Win32_System_Pipes",
    # `GetProcessMemoryInfo`, so `tile-bench` can report a peak on Windows
    # instead of the `NaN` that stands in for "not measurable here".
    "Win32_System_ProcessStatus",
    "Win32_System_SystemServices",
    "Win32_System_Threading",
    # For `CommandLineToArgvW`, used by a test as an *independent* parser of the
    # command line `Worker::spawn_mapped` builds --- AGENTS.md records that a
    # writer and its own reader agree about output that is wrong.
    "Win32_UI_Shell",
] }

# The Windows print path, `print_win.rs`. Two things are needed that `windows-sys`
# structurally cannot give:
#
# * **WinRT.** `Windows.Data.Pdf` is the operating system's own PDF parser and
#   rasteriser --- the direct counterpart of PDFKit on macOS, and the one Windows
#   itself uses for PDF thumbnails and preview. `windows-sys` binds Win32 only, so
#   reaching it needs the projected `windows` crate and `windows-future` for the
#   async operations. It is the *independent* parser this repository's print
#   checks are built around: `lopdf` writes the job and PDFium drew what the reader
#   saw, so neither can say the output is readable by something else.
# * **GDI printing.** Windows has no in-box "print this PDF" API at any layer, so
#   the job is rasterised page by page onto a printer DC, which is what every
#   Windows PDF viewer does. `PrintDlg` for the panel, `StretchDIBits` for the page.
#
# Same licensing and no-new-crate argument as `windows-sys` above: `windows 0.61`
# is already in `Cargo.lock` transitively through Tauri's WebView2 stack, with
# `windows-future` and `windows-core` beside it, and it is `MIT OR Apache-2.0`.
windows = { version = "0.61", features = [
    # PdfDocument, PdfPage, PdfPageRenderOptions.
    "Data_Pdf",
    # Blocking on the WinRT async operations the loader returns.
    "Foundation",
    # `BitmapEncoder::BmpEncoderId`, which is the load-bearing choice here: asking
    # for BMP rather than the default PNG means the rendered page arrives as a DIB
    # that `StretchDIBits` accepts as-is, so the print path needs no image decoder
    # and gains no dependency for one.
    "Graphics_Imaging",
    # InMemoryRandomAccessStream and DataWriter, to get bytes in and out of WinRT.
    "Storage_Streams",
    "Win32_Foundation",
    # StretchDIBits, GetDeviceCaps, CreateDC, DeleteDC.
    "Win32_Graphics_Gdi",
    # StartDoc/StartPage/EndPage/EndDoc and DOCINFOW. Not in `Graphics_Gdi`, which
    # is where they read as though they belong: Windows files the document-level
    # printing calls under the XPS print stack even though they are the same
    # decades-old GDI escape sequences.
    "Win32_Storage_Xps",
    # Toolhelp, so `print-probe` can read its own module table and say which PDF
    # parser the printing process actually mapped -- see `boundary` there.
    "Win32_System_Diagnostics_ToolHelp",
    # PrintDlgW, and the CommDlg error code that distinguishes Cancel from failure.
    "Win32_UI_Controls_Dialogs",
] }
tauri-plugin-single-instance = "2.4.3"

[[example]]
name = "tile-bench"
path = "examples/tile_bench.rs"

[[example]]
name = "text-roundtrip"
path = "examples/text_roundtrip.rs"

[[example]]
name = "annot-probe"
path = "examples/annot_probe.rs"

[[example]]
name = "helvetica-probe"
path = "examples/helvetica_probe.rs"

[[example]]
name = "crop-probe"
path = "examples/crop_probe.rs"

[[example]]
name = "remove-probe"
path = "examples/remove_probe.rs"

[[example]]
name = "sanitize-rewrite"
path = "examples/sanitize_rewrite.rs"

[[example]]
name = "worker-bench"
path = "examples/worker_bench.rs"

[[example]]
name = "ocr-probe"
path = "examples/ocr_probe.rs"

[[example]]
name = "latency-bench"
path = "examples/latency_bench.rs"

[[example]]
name = "incremental-save"
path = "examples/incremental_save.rs"

[[example]]
name = "thread-probe"
path = "examples/thread_probe.rs"

[[example]]
name = "text-probe"
path = "examples/text_probe.rs"

[[example]]
name = "progressive-probe"
path = "examples/progressive_probe.rs"

[[example]]
name = "outline-probe"
path = "examples/outline_probe.rs"

[[example]]
name = "comments-probe"
path = "examples/comments_probe.rs"

[[example]]
name = "links-probe"
path = "examples/links_probe.rs"

[[example]]
name = "signature-probe"
path = "examples/signature_probe.rs"

[[example]]
name = "search-probe"
path = "examples/search_probe.rs"

[[example]]
name = "structure-probe"
path = "examples/structure_probe.rs"

[[example]]
name = "worker-probe"
path = "examples/worker_probe.rs"

[[example]]
name = "backend-probe"
path = "examples/backend_probe.rs"

[[example]]
name = "pool-bench"
path = "examples/pool_bench.rs"

[[example]]
name = "prespawn-bench"
path = "examples/prespawn_bench.rs"

[[example]]
name = "fdpass-probe"
path = "examples/fdpass_probe.rs"

[[example]]
name = "win-sandbox-probe"
path = "examples/win_sandbox_probe.rs"

[[example]]
name = "print-probe"
path = "examples/print_probe.rs"

[[example]]
name = "rewrite-probe"
path = "examples/rewrite_probe.rs"
